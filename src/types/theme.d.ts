
export interface Theme {
  accent: string;
  textColor: string;
  background: string;
  foreground: string;
  primary: string;
  cardBackground: string;
  inputBackground: string;
  mutedTextColor: string;
}
